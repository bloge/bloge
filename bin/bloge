#!/usr/bin/env php
<?php

require dirname(__DIR__) . '/vendor/autoload.php';

$file = realpath($_SERVER['argv'][1]);
$destination = $_SERVER['argv'][2];

$app = require $file;

$builder = new Bloge\Compilers\HTMLCompiler($app);
$builder->build($destination);